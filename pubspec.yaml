name: uts_ui_search_page_ig
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  material_symbols_icons: ^4.2719.3

    # flutter_svg: ^0.22.0
    # flutter_icons: ^1.1.0
    # keyboard_avoider: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/img/
